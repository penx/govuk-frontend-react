import React from 'react';
import Input from '.';

export default (
  <Input
    label={{ children: 'Label' }}
    hint={{ children: 'Hint' }}
    errorMessage={{ children: 'Error' }}
    id="test"
  />
);
